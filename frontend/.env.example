# Dev server proxies /api -> backend (see vite.config.js). Override if needed.
VITE_API_URL=/api

# Google OAuth client id for student login. Leave empty to hide the Google button.
VITE_GOOGLE_CLIENT_ID=
